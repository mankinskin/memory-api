[package]
name = "doc-api"
version = "0.1.0"
edition = "2024"
description = "Documentation workspace model and cargo metadata ingestion"

[dependencies]
cargo_metadata = "0.19"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
pretty_assertions = "1"
tempfile = "3"
