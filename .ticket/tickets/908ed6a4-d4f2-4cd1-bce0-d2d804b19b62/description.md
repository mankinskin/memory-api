## Problem\n\nFollow-up from the review of  (closed done). Two gaps were waived into this ticket:\n\n1.  and  are not explicitly round-trip-asserted. [snapshot_and_handoff.rs](memory-api/crates/session-api/src/store_tests/workflow/snapshot_and_handoff.rs)  asserts objective / target_tickets / target_files / decisions / non_goals / context_anchors / open_escalations — but not those two.\n2.  AC4 ( inversion resolved/documented) and AC5 (old inline packages still readable) were never independently verified.\n\nRisk is low — the struct is a generic serde copy with no field-specific logic — but the assertion set claims per-parameter coverage it does not have.\n\n## Acceptance criteria\n\n1.  and  are asserted with non-default values in a handoff round-trip test.\n2. Every field accepted by  has a round-trip assertion; add a comment or test-name convention making that contract explicit.\n3. The  inversion is either resolved in code or documented with rationale, and the outcome is recorded on this ticket.\n4. A test reads an old inline handoff package and confirms it still deserializes (back-compat).\n\n## Context\n\nNote for whoever picks this up: the original " is being dropped" premise was a **serde artifact**, not a bug.  at [handoff.rs:41](memory-api/crates/session-api/src/model/handoff.rs#L41) omits the key from JSON output only when the vector is empty. Verified:  (2 non-empty entries) passes, and  passes. Do not re-open that thread.\n\nStatus after implementation:\n\n- Validation command: cargo test: 193 passed (10 suites, 5.15s)\n- Result: passed\n- AC3 outcome: no session-api code fix was needed; there is no live  inversion in this crate, and the apparent lossiness was the existing serde behavior for empty . The back-compat check now covers the old inline handoff package shape.\n\n## Non-goals\n\n- Changing the handoff schema.\n- Changing serde attributes.\n