## Validation — guidance refresh (2026-07-30)

AC1 (no instruction tells writes into description): met. Grep for `"new"|"ready"|state=new|state=ready|description.md|description_mode` across AGENTS.md, .agents/instructions, .agents/prompts, .clinerules found zero stale-state hits (state rename already fully propagated by ticket 5b3da351) and one false positive (`third-party.md:241` uses "ready" for service readiness, unrelated). review.agent.md step 8 changed from "ticket field patches" to `write_part kind=review`; testing.agent.md and implement.agent.md updated to name `validation`/`plan` respectively.

AC2 (four view profiles documented): met — added table (summary/plan/review/full) to workflow.instructions.md `### Reading Tickets: View Profiles and Parts`, sourced from ticket-api/src/model/parts.rs `ViewProfile`.

AC3 (freeze contract + both recoveries): met — lifecycle.instructions.md `## Plan Freezing at planned` documents the 5 frozen kinds, hard-reject behavior, amendment (`write-amendment --supersedes`) and re-plan (transition to `open`) recovery paths.

AC4 (rule-backed files regenerated, no diff): workflow.instructions.md and lifecycle.instructions.md are NOT rule-generated targets (absent from rule-targets.yaml / memory-api/rule-targets/*.yaml — confirmed by grep). Root AGENTS.md (hand-authored, no `rule-api:file generated=true` marker) is also not a generated target; memory-api/AGENTS.md IS rule-generated but its ticket-lifecycle section already delegates to the root as canonical, so no drift was introduced by leaving it untouched. rule-mcp is not in this agent's tool grant (Implement Agent excludes rule-mcp/context-mcp/session-mcp/audit-mcp per mode instructions), so no rule entries were edited — this AC is vacuously satisfied for the touched files (none are rule-backed) but the rule-mcp regeneration step itself was not executable in this session; flagged as a scope limitation, not a failure.

AC5 (Review/Testing/Implement name their part kind): met — review.agent.md -> `review`, testing.agent.md -> `validation`, implement.agent.md -> `plan` (read).

AC6 (fresh session can record a review on a planned ticket without freeze rejection): met by construction — review/validation/notes/amendment kinds are excluded from PLANNING_PART_KINDS in parts.rs, and the new docs instruct writing those kinds instead of the frozen `objective`/`acceptance_criteria` kinds. Not re-verified end-to-end against a live `planned` ticket in this session (doc-only change, no ticket-api code touched).

AC7: this part itself is the recorded validation execution, linked to ticket 71e13480 via a `validation`-kind part (test-api MCP execution recording was not available/used here; the ticket-side part is the evidence artifact per ticket instructions).

Files changed (all hand-authored, none rule-generated): AGENTS.md, .agents/instructions/ticket/workflow.instructions.md, .agents/instructions/ticket/lifecycle.instructions.md, .agents/agents/review.agent.md, .agents/agents/testing.agent.md, .agents/agents/implement.agent.md.