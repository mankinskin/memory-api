Iteration review (this session) — returned to in-implementation.

FAIL (partial): defect 2 (null-value TOML deletion corruption) is fully fixed and verified via passing tests. Defect 1 ("silent history-append failures") is fixed at exactly one call site (store.rs L543, the `update_ticket` path) but the ticket's own "Not satisfied: None" claim overstates completeness — 8 structurally identical `let _ = TicketFs::append_history(...)` sites remain in the same crate:
- store.rs L302 (ticket creation)
- store/lifecycle.rs L262 (attach)
- store/parts.rs L94, L137, L198 (write_part, write_amendment_part, undo_part — the new part-write machinery from 3d952036/f9e70385)
- store/query.rs L178, L216 (add_edge/remove_edge)

User decision: fix all remaining sites (not just open a follow-up ticket) and correct the ticket's "Not satisfied: None" claim to accurately reflect what was fixed before this rework. Re-verify each site surfaces a tracing::error! (or equivalent) on append failure instead of silently discarding the Result.